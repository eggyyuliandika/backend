'use strict';

/**
 * waste-exchange controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::waste-exchange.waste-exchange');
